let myName = "OtAvIo";

console.log(myName);

// Create a function that takes a string as input and returns the same string with all letters in uppercase.

function convertNameToUpperCasse(name) {
  return name.toUpperCase();
}

console.log(convertNameToUpperCasse("otavio"));

// Create another function that takes a string as input and returns the same string with all letters in lowercase.

function convertNameToLowerCase(name) {
  return name.toLowerCase();
}

console.log(convertNameToLowerCase(myName));
